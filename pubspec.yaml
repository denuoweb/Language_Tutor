name: language_tutor
description: "Ambient SRS Language Tutor."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  camera: ^0.12.0+1
  drift: ^2.32.1
  firebase_ai: ^3.11.0
  firebase_core: ^4.7.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  flutter_tts: ^4.2.5
  json_annotation: ^4.11.0
  path: ^1.9.1
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  uuid: ^4.5.3

dev_dependencies:
  build_runner: ^2.15.0
  drift_dev: ^2.32.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  json_serializable: ^6.13.2

flutter:
  uses-material-design: true
